# Core backend framework
Django==5.1.7
djangorestframework==3.15.2
djangorestframework-simplejwt==5.5.0
django-cors-headers==4.7.0
python-dotenv==1.0.1
psycopg[binary]==3.2.13
gunicorn==23.0.0

# Market data, analytics, forecasting
numpy==2.2.4
pandas==2.2.3
scikit-learn==1.6.1
statsmodels==0.14.4
yfinance==0.2.54
requests==2.32.3

# Sentiment, reporting, auth helpers
reportlab==4.2.5
textblob==0.19.0
vaderSentiment==3.3.2
bcrypt==4.2.1

# FinBERT sentiment path (used when FINBERT_ENABLED=true)
transformers==5.3.0
torch==2.9.1

# Assistant module (RAG / LLM integrations)
# ChromaDB is optional: if missing, assistant vector store falls back to local JSON/memory mode.
# langgraph is not used in this project.
# chromadb==0.5.5

# Optional for the UMAP path in clustering_module.stock_clustering_service
# umap-learn==0.5.7
